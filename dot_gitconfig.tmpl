[url "ssh://git@github.com/"]
	insteadOf = https://github.com/
[user]
	name = tauraamui
	email = adampstringer@protonmail.com
{{- if or (eq .chezmoi.hostname "Adams-MBP") (eq .chezmoi.hostname "penguin") }}
	{{- if (eq .chezmoi.hostname "Adams-MBP") }}
	signingkey = 1062B59D5867C843
	{{- end }}
	{{- if (eq .chezmoi.hostname "penguin") }}
	signingkey = 2C6AB603B3FCD013
	{{- end }}
[commit]
	gpgsign = true
{{- end }}
